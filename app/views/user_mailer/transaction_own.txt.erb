Obrigado por utilizar nosso sistema de compras!


Olá <%= @order.user.nome%>.

Segue os dados para depósito/transferência.

Banco Itau
Agência: 0007
Conta Corrente: 02939-0
Nome: Licell Comercio e Serviços de Eletrônicos


Seu pedido:

Quantidade		Produto		Valor Unitário		Sub Total
<% 
	$subtotal = 0
	$valor_total = 0
	@order_itens.each do |item|
%>			
<%= item.amount %>		<%= item.product.name %>		<%= number_to_currency(item.price / item.amount) %>		<%= number_to_currency($subtotal = (item.price)) %>

<%
	$valor_total += $subtotal
	end
%>


Total	<%= number_to_currency($valor_total) %>


Tipo de pagamento:</b> <%= traduz_tipo_do_pagamento(@order.payment_type.to_s.strip)%>
